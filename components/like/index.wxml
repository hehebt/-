<!--components/like/index.wxml-->
<view class="container" bind:tap="onLike">
    <image class="like" src="{{like? yesSrc : noSrc}}" mode="widthFix"></image>
    <text class="">{{count}}</text>
</view>
